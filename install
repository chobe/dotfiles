#!/usr/bin/env bash

folder=(bin nvim tmux uwuntu zsh)
DOTFILES=$HOME/personal/dotfiles/main

pushd $DOTFILES

for i in "${folder[@]}"
do
  echo "stow $i"
  stow -D $i
  stow $i --target=$HOME
done

popd
